program Sort(input, output);
type
    Str25    = string;
    TBookRec = int;
var    
    myVar   : int;
    i,j : int;
begin
 i:=0;
 writeln('** Calcular la raíz cuadrada de 12 **');
 writeln('Entrar x (> 0): ');
 while (true) do
    begin
        myVar := 5+5;
        Str25 := 'Hola Mundo';
        for j := 0 to 5 do
        begin
            writeln('** Calcular la raíz cuadrada de 12 **');
        end
    end
end.